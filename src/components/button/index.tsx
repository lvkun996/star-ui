import React from 'react'

import { Button } from '@tarojs/components'

import { SaButtonProps } from '../../types/button'

const StarButton: React.FC<SaButtonProps> = ({
  
}) => {

  return (
    <Button >
    
    </Button>
  )
} 

export default StarButton
